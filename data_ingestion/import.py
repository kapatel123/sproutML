import pandas as pd

# TODO determine whether a class makes sense. pd.read_csv takes care of .csv and .txt. Assuming those are primary file types. hdfs also has premade method.
# Class likely best for splitting import for multiprocessing needs


class DataImporter:
    pass
